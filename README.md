# Favocado for fuzzing Chromium

### Prerequisites
- Node

### Usage:

#### Generate Testcase 
* Generate html copus:
    `node .\Generator\Run\Gen.js -o output -n 10`

#### Context-dependent Fuzzing.

Config Logger and run similar to fuzzing webkitgtk++
